<!--
GroveSpec restructuring — brownfield backlog of TREE-LEVEL problems: places the structure itself is wrong
(no clear entry point · a god-module doing everything · a circular dependency · dead code).
The tree maps the code HONESTLY as-is now — even if ugly; never fabricate a clean shape it doesn't have
(a tree that lies about the code is the drift GroveSpec exists to stop, inherited by every later task).
This file records where it SHOULD go. Resolve via `grovespec-revise` structure change (split · merge · move),
then check it off. Created only when there's something to record. Content in config.language.
Node-level bugs · duplications go in findings.md, not here.
-->

- [ ] {what's structurally wrong} @ {region or TASK-N} → {proposed split · merge · move}
